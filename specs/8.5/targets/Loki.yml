Loki:
  type: object
  required:
    - expr
  properties:
    expr:
      type: string
    legendFormat:
      type: string
      default: ''
      description: Defines the legend.
    refId:
      type: string
      readOnly: true
